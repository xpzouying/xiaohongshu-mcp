 方式一：Python 快速测试（用运行中的 MCP 服务，无需构建）

  把测试脚本存成文件方便复用：

● Write(cmd\scheduler\test_run.py)
  ⎿  Wrote 97 lines to cmd\scheduler\test_run.py
       1 #!/usr/bin/env python3
       2 """
       3 快速测试脚本：调用运行中的 MCP 服务 + DeepSeek + 飞书
       4 关键词从 keywords.txt 读取，也可用 -k 参数覆盖
       5
       6 用法:
       7   python test_run.py
       8   python test_run.py -k 申论低分
       9 """
      10 import argparse, datetime, os, requests, sys
     … +91 lines (ctrl+o to expand)

● 调用命令：

  # 用 keywords.txt 里的关键词
  python cmd/scheduler/test_run.py

  # 临时换关键词（不改文件）
  python cmd/scheduler/test_run.py -k 陕西申论压分

  # 指定最低点赞数
  python cmd/scheduler/test_run.py -k 申论低分 --min-likes 500

  ---
  方式二：Docker 构建 Go 二进制（验证编译）

  docker run --rm -v "D:/03code/xiaohongshu-mcp:/src" -w /src -e GOPROXY=https://goproxy.cn,direct golang:1.24 go build
  ./cmd/scheduler/

  ---
  日常用方式一就够了，改关键词只需编辑 cmd/scheduler/keywords.txt。